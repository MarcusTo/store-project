export enum ROUTE_PATHS {
  HOME = "/",
  
  PRODUCTS = "/products",
  PRODUCT_DETAILS = "/products/:id",

  CART = "/cart",
  CHECKOUT = "/checkout",
  
  LOGIN = "/login",
  REGISTER = "/register",
    
  ORDERS = "/orders",
  
  ABOUT = "/about",
  CONTACT = "/contact",
}

export default { ROUTE_PATHS };
